// import { tasks as data } from "@/assets/data/api/data";
import { Task, TaskData } from "@types";

function deleteTaskById(id: number): number {
  // const taskIndex = data.findIndex((task: Task) => task.id === id);
  // if (taskIndex === -1) {
  //   throw new Error("Task not found");
  // }
  // data.splice(taskIndex, 1);
  // return id;
	return 1;
}

export default deleteTaskById;
